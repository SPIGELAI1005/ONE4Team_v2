# ONE4Team — PHASE 3 INDEX (Matches + football stats)

Goal: football-specific differentiator + sticky daily usage.

Deliverables:
- match creation
- match events logging
- leaderboards + player profiles

---

## Supabase bundle + checklist
- SQL bundle: `supabase/APPLY_BUNDLE_PHASE3.sql`
- Checklist: `supabase/APPLY_CHECKLIST_PHASE3.md`

Recommended apply order (fresh project):
1) `supabase/APPLY_BUNDLE_BASELINE.sql`
2) `supabase/APPLY_BUNDLE_PHASE1.sql`
3) `supabase/APPLY_BUNDLE_PHASE0_RLS.sql`
4) `supabase/APPLY_BUNDLE_PHASE2.sql`
5) `supabase/APPLY_BUNDLE_PHASE3.sql`

---

## App implementation (already present)
- Matches page: `src/pages/Matches.tsx`
- Player stats: `src/pages/PlayerStats.tsx`
- Player profile: `src/pages/PlayerProfile.tsx`

---

## Phase 3 exit criteria (local readiness)
- Bundle + checklist present ✅
- Matches + stats UI present ✅
- `npm run lint` / `npm test` / `npm run build` ✅
- `npm run audit:phase0` ✅

## Phase 3 exit criteria: PASS (local readiness)
- Bundle + checklist present ✅
- Matches + stats UI present ✅
- `npm run lint` / `npm test` / `npm run build` ✅
- `npm run audit:phase0` ✅
