/**
 * Hover styles aligned with app `Button` variants (`accent` / crimson in index.css).
 * Use `!` on fill hovers so they win over inline `backgroundColor` / `color` from club branding.
 */
export const clubCtaFillHoverClass = "transition-colors hover:!bg-accent hover:!text-accent-foreground";

export const clubCtaOutlineHoverClass =
  "transition-colors hover:border-accent/40 hover:bg-accent/10 hover:text-accent-foreground";
