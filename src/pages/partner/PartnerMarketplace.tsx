import ProviderMarketplacePortal from "@/components/marketplace/provider-marketplace-portal";

/** Partner marketplace — discover club requests, send offers, build collaborations. */
export default function PartnerMarketplacePage() {
  return <ProviderMarketplacePortal />;
}
